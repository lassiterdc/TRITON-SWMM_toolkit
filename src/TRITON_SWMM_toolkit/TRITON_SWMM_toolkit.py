def run_model(config_path, verbose=False, timestep=5):
    """Dummy run_model for testing GUI/CLI."""
    print(f"Running simulation with config: {config_path}")
    print(f"Verbose: {verbose}, Timestep: {timestep}")
    # Simulate a result
    return {"status": "success"}
